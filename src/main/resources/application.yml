spring:
  application:
    name: ai-repo
  flyway:
    enabled: false
  sql:
    init:
      mode: never

server:
  port: 8080

ai-repo:
  ops:
    admin-token: ${AI_REPO_OPS_ADMIN_TOKEN:local-ops-token}
